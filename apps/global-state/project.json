{
  "root": "apps/global-state",
  "sourceRoot": "apps/global-state",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/global-state",
        "outputPath": "dist/apps/global-state"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "global-state:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "global-state:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "global-state:build:production"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/global-state/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
